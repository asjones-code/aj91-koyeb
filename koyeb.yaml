name: aj91-koyeb
# Note: If deploying as static site, serverless functions may need separate configuration
# For now, the frontend falls back to rss2json/CORS proxies if /api/writing-feed is unavailable
