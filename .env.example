# OpenAI API key for the terminal "ask" command (client-side; restrict key by domain in OpenAI dashboard).
#
# Local: copy to .env and set your key, then restart dev server.
#   cp .env.example .env
#   # edit .env: OPENAI_API_KEY=sk-...
#   npm run dev
#
# Or in shell before starting:
#   export OPENAI_API_KEY=sk-...
#   npm run dev
#
# Koyeb (prod):
# - Netlify: use netlify/functions/ask.js; set OPENAI_API_KEY (and optional SITE_ORIGIN) in Netlify env.
# - Koyeb: run "npm run build && npm run serve". Set OPENAI_API_KEY and SITE_ORIGIN (e.g. https://your-app.koyeb.app) in Koyeb env.
OPENAI_API_KEY=
