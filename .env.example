# Dev: copy to .env. Prod (Koyeb): set OPENAI_API_KEY in service env/secrets.
#
# Dev workflow (two terminals):
#   1. npm run start     → Node server (dist + /api/ask) on port 3000, loads .env
#   2. npm run dev       → Parcel on port 1234; set API_ORIGIN below so /api/ask hits Node
# Prod: npm run build && npm run start; OPENAI_API_KEY from Koyeb env.
OPENAI_API_KEY=
# Dev only: base URL of the Node server so Parcel app can call /api/ask (e.g. http://localhost:3000).
API_ORIGIN=http://localhost:3000
DATABASE_URL=
# PM POC: password for /pm.html project management (optional).
PM_PASSWORD=
# MailerSend API token for task assignment + project invite emails (optional).
# Get from https://www.mailersend.com - add to .env for testing (never commit).
MAILERSEND_API_TOKEN=
# Sender email (must be from a verified domain in MailerSend).
MAILERSEND_FROM_EMAIL=noreply@yourdomain.com
MAILERSEND_FROM_NAME=PM
# Base URL for verification links (e.g. https://yoursite.com or http://localhost:3000).
APP_BASE_URL=http://localhost:3000
