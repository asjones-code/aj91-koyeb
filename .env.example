# OpenAI API key for the terminal "ask" command (client-side; restrict key by domain in OpenAI dashboard).
#
# Local: copy to .env and set your key, then restart dev server.
#   cp .env.example .env
#   # edit .env: OPENAI_API_KEY=sk-...
#   npm run dev
#
# Or in shell before starting:
#   export OPENAI_API_KEY=sk-...
#   npm run dev
#
# Koyeb (prod): App → Environment variables → add OPENAI_API_KEY.
# Option A (runtime): Run the app with server.js so the key is injected per request.
#   Build: npm run build
#   Run: npm run serve  (or node server.js)
#   Set OPENAI_API_KEY in Koyeb runtime env.
# Option B (build): Set OPENAI_API_KEY in Build env so Parcel inlines it (no server).
OPENAI_API_KEY=
