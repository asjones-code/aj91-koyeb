# OpenAI API key for the terminal "ask" command (client-side; restrict key by domain in OpenAI dashboard).
#
# Local: copy to .env and set your key, then restart dev server.
#   cp .env.example .env
#   # edit .env: OPENAI_API_KEY=sk-...
#   npm run dev
#
# Or in shell before starting:
#   export OPENAI_API_KEY=sk-...
#   npm run dev
#
# Koyeb (dev/prod): In the Koyeb dashboard → your App → Environment variables,
# add OPENAI_API_KEY with your key. Use "Build" scope if your build step needs it
# (key gets inlined into the bundle at build time).
OPENAI_API_KEY=
